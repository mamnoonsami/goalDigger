-- =====================================================
-- Seed data for local development / testing
-- Run AFTER migrations. Uses Supabase-generated UUIDs.
-- =====================================================

-- NOTE: Auth users must be created manually via Supabase Auth
-- or via the Supabase dashboard before seeding profiles.
-- These are example profiles for reference only.

-- Example: promote the first registered user to admin
-- UPDATE profiles SET role = 'admin' WHERE id = '<your-user-uuid>';
